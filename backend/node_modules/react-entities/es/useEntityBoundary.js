import { useEffect } from 'react';
import store from './store';
export var useEntityBoundary = function useEntityBoundary() {
  useEffect(function () {
    return function () {
      for (var i = 0; i < store.length; i++) {
        store[i].reset();
      }
    };
  }, []);
};
export default useEntityBoundary;